# Reads secrets from AWS Secrets Manager using the controller's Pod Identity credentials.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
  conditions:
    - namespaces: [backend, database]
