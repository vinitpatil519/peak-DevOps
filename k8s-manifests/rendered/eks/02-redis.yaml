# GENERATED by scripts/render-manifests.sh from helm-charts/charts/redis
# with gitops/environments/eks/redis.yaml. Do not edit by hand.
---
# Source: redis/templates/redis.yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels: { kubernetes.io/metadata.name: backend }
      ports: [{ port: 6379, protocol: TCP }]
    - from:
        - namespaceSelector:
            matchLabels: { kubernetes.io/metadata.name: monitoring }
      ports: [{ port: 9121, protocol: TCP }, { port: 15020, protocol: TCP }]
  egress: []

---
# Source: redis/templates/redis.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis

---
# Source: redis/templates/redis.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: false

---
# Source: redis/templates/redis.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
data:
  redis.conf: |
    bind 0.0.0.0
    port 6379
    protected-mode yes
    maxmemory 400mb
    maxmemory-policy allkeys-lru
    appendonly no
    save ""
    dir /data
    tcp-keepalive 60
    timeout 300
    # Disable dangerous commands for application clients.
    rename-command FLUSHALL ""
    rename-command FLUSHDB ""
    rename-command DEBUG ""
    # CONFIG stays enabled: redis_exporter reads maxmemory settings through CONFIG GET.

---
# Source: redis/templates/redis.yaml
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
  ports:
    - { name: tcp-redis, port: 6379, targetPort: redis, appProtocol: redis }
    - { name: http-metrics, port: 9121, targetPort: metrics }

---
# Source: redis/templates/redis.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  serviceName: redis
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis
  template:
    metadata:
      labels:
        helm.sh/chart: redis-1.0.0
        app.kubernetes.io/name: redis
        app.kubernetes.io/instance: redis
        app.kubernetes.io/version: "8.2-alpine"
        app.kubernetes.io/component: cache
        app.kubernetes.io/part-of: cloudforge
        app.kubernetes.io/managed-by: Helm
      annotations:
        checksum/config: b91b3e3d1998b61d3ff956a8eea1344121a37a99779608b9f0eadea87d4c9b75
        sidecar.istio.io/inject: "true"
    spec:
      serviceAccountName: redis
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: redis
          image: "redis:8.2-alpine"
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "exec redis-server /etc/redis/redis.conf --requirepass \"$REDIS_PASSWORD\""]
          env:
            - name: REDIS_PASSWORD
              valueFrom: { secretKeyRef: { name: redis-credentials, key: password } }
            - { name: REDISCLI_AUTH, valueFrom: { secretKeyRef: { name: redis-credentials, key: password } } }
          ports:
            - { name: redis, containerPort: 6379, protocol: TCP }
          readinessProbe:
            exec: { command: ["sh", "-c", "redis-cli -h 127.0.0.1 ping | grep -q PONG"] }
            periodSeconds: 5
            timeoutSeconds: 2
          livenessProbe:
            tcpSocket: { port: redis }
            periodSeconds: 10
            failureThreshold: 3
          resources: 
            limits:
              memory: 600Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: config, mountPath: /etc/redis, readOnly: true }
            - { name: data, mountPath: /data }
        - name: exporter
          image: oliver006/redis_exporter:v1.77.0
          env:
            - { name: REDIS_ADDR, value: "redis://127.0.0.1:6379" }
            - name: REDIS_PASSWORD
              valueFrom: { secretKeyRef: { name: redis-credentials, key: password } }
          ports:
            - { name: metrics, containerPort: 9121, protocol: TCP }
          resources:
            requests: { cpu: 10m, memory: 16Mi }
            limits: { memory: 64Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: { drop: [ALL] }
      volumes:
        - name: config
          configMap: { name: redis }
        - { name: data, emptyDir: { sizeLimit: 256Mi } }

---
# Source: redis/templates/redis.yaml
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/backend/sa/backend-api
      to:
        - operation: { ports: ["6379"] }
    - to:
        - operation: { ports: ["9121"] }

---
# Source: redis/templates/redis.yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-credentials
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secrets-manager
  target: { name: redis-credentials, creationPolicy: Owner }
  data:
    - secretKey: password
      remoteRef: { key: cloudforge/dev/app, property: redis_password }

---
# Source: redis/templates/redis.yaml
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: redis-metrics
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis
  mtls: { mode: STRICT }
  portLevelMtls:
    "9121": { mode: PERMISSIVE }

---
# Source: redis/templates/redis.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis
  labels:
    helm.sh/chart: redis-1.0.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
    app.kubernetes.io/version: "8.2-alpine"
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: cloudforge
    app.kubernetes.io/managed-by: Helm
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis
  endpoints:
    - { port: http-metrics, interval: 30s }
