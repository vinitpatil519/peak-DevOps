---
galaxy_info:
  role_name: jenkins
  author: cloudforge
  description: Jenkins LTS (JCasC) + SonarQube CI stack on Docker
  license: MIT
  min_ansible_version: "2.16"
dependencies:
  - role: docker
