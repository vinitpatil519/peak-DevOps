---
# Jenkins build host on EC2 (optional). Reach it through SSM Session Manager or a bastion.
all:
  children:
    ci:
      hosts:
        jenkins-01:
          ansible_host: 10.40.1.10
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/cloudforge-ci.pem
    workstation:
      hosts: {}
  vars:
    kube_context: cloudforge-eks
